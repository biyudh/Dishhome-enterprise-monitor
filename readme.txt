# Dishhome Enterprise Monitor

A Flask application for monitoring enterprise systems.

## Installation
```bash
pip install -r requirements.txt
==========================

1️⃣ Don’t use plain sudo — use sudo with the venv Python // when ping not working.

Then run your app with:

sudo /home/yudha/ent-monitor/monitoring_dashboard_v1.1/venv/bin/python app.py


This ensures sudo uses the venv Python which has Flask installed.k
